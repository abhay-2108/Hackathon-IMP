{
	"installCommand": "(npm install) && (python3 -m pip install --upgrade pip) && (python3 -m pip install -r requirements.txt -t python_modules)",
	"buildCommand": "npm run build",
	"functions": {
		"app/api/**": {
			"runtime": "nodejs20.x"
		}
	},
	"env": {
		"PYTHON_PATH": "python3",
		"PYTHONUSERBASE": "python_modules"
	}
}
